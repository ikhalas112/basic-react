import React from "react";

export default function BasicTsx() {
  // const element = <h1>Hello, world!</h1>;

  // const name = "Josh Perez";
  // const element = <h1>Hello, {name}</h1>;

  // function formatName(user: any) {
  //   return user.firstName + " " + user.lastName;
  // }

  // const user = {
  //   firstName: "Harper",
  //   lastName: "Perez",
  // };

  // const element = <h1>Hello, {formatName(user)}!</h1>;

  // const element = <img src={user.avatarUrl}></img>;

  // const bool = true;
  // const element = <h1>{!bool ? "this is true" : "this is false"}</h1>;

  return <div style={{ color: "red", fontSize: 24 }}>with Style</div>;

  // return <div className="container welcome-text">{element}</div>;
}
